function setup () {
  createCanvas(windowWidth, windowHeight)
}

function draw () {
  background(255, 255, 255)
  fill(234, 2, 32)
  textAlign(CENTER, CENTER)
  textSize(33)
  text('you are gae', 100, 100)
}

export { setup, draw }
